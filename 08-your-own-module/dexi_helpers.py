#!/usr/bin/env python3
"""
dexi_helpers — your reusable DEXI flight helpers.

TODO: move connect, takeoff_to, wait_until_altitude, and safe_land into this file.
"""

import asyncio
from mavsdk import System
from mavsdk.offboard import OffboardError, VelocityBodyYawspeed


# TODO: write `async def connect(address):` that returns a connected drone


# TODO: write `async def wait_until_altitude(drone, target_m, tolerance=0.3):`


# TODO: write `async def takeoff_to(drone, altitude_m):`


# TODO: write `async def safe_land(drone):`
